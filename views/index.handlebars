<div class="container">

<div class="text-center">
<img class="py-4" src="/assets/img/burger.png" alt="Burger Picture">
</div>

<div class="row">
<div class="col-sm">
<h2>Burgers that I haven't eaten:</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>

<div class="col-sm">
<h2>Burgers that I have eaten:</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>
</div>
</div>


<h2 class="d-flex justify-content-center pt-5">Add a burger:</h2>
<div class="d-flex justify-content-center">
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Burger Status:?</label><br>
    <input type="radio" name="devoured" value="1" checked> Eaten.<br>
    <input type="radio" name="devoured" value="0"> To be Eaten.
  </div>

  <button class="btn btn-primary" type="submit">Add A Burger</button>
</form>
</div>

</div>